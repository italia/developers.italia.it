/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /**
   * Export the app as a static site as production deployment and
   * hosting is done using GitHub Pages.
   */
  output: 'export'
}

export default nextConfig
